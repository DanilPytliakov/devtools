# Quick Start:

[![Java CI with Checkstyle and JaCoCo](https://github.com/DanilPytliakov/devtools/actions/workflows/ci.yml/badge.svg)](https://github.com/DanilPytliakov/devtools/actions/workflows/ci.yml)

## Как запускать Gradle-задачи из IDE
1) Откройте View → Tool Windows → Gradle.
2) В дереве задач выберите Tasks → application → run, Tasks → build → build или Tasks → verification → test, запускай двойным кликом.
## Альтернативный способ
Даблкликом Ctrl + Ctrl открываем меню (Run Anything) и вводим gradle run или gradle test — IDEA выполнит команду без терминала.

## Таблица переменных/полей класса MenteeProgress:
| Имя перменной       | Тип    | Описание   |
|---------------------|--------|----------------------|
| menteeName          | String | Имя студента         |
| sprintNumber        | int    | Номер спринта        |
| plannedHoursPerWeek | int    | Запланированные часы |

## Зачем нужен пакет ru.mentee.power. ?
Вышеупомянутый пакет, как и всех пакеты в Java: являются механизмом организации классов, интерфейсов и других типов в функциональные группы (папки),
необходимым для предотвращения конфликтов имен, управления областью видимости (контроля доступа), структурирования кода, а также для удобства поиска и использования библиотек.

## Принцип работы метода readyForSprint() 
Это метод без аргументов возвращает тип boolean: либо true, либо false. Это ответ на вопрос «готов ли студент к спринту?». 
Внутри return plannedHoursPerWeek >= 3; сравнивает запланированные часы с порогом 3.

## Правило веток feature/DVT-X
В Dev Tools оставляем master чистым, работу ведём в ветке feature/DVT-<номер>; это упрощает проверку и дальнейший PR. Подход соответствует lightweight Git Flow.
## Git локальный цикл
Локальный репозиторий (Local Repository): Это место на вашем компьютере, где хранится вся история ваших коммитов (снимков состояния проекта). Команда git commit создает новый коммит, который фиксирует все изменения, находящиеся в индексе, и добавляет его в историю локального репозитория.

## Перед push: git status clean
1) Локальная диагностика перед правками: всегда смотрите git status и вкладку Commit в IDE — картинка должна совпадать. Это предотвращает случайные коммиты.
2) Малый санитарный коммит: изолируйте «чистку» от функциональных изменений. История будет понятнее, откаты — безопаснее.
3) Поддержка .gitignore как живого документа: добавляйте новые паттерны, когда появляется новый инструмент (например, новый репорт‑каталог).
4) Инвариант «status clean перед push»: фиксируем в Runbook и следуем ему. Это критерий допуска к ревью.
5) Симметрия UI/CLI: проверяйте, что Commit Tool Window и git status показывают одно и то же — иначе ищем причину рассинхрона.

## Кодстайл-гайд проекта devtools

Проект следует правилам Google Java Style Guide с адаптацией.
Автоматическая проверка: ./gradlew checkstyleMain

### 1. Именование методов: camelCase

До:    public void add_student(Student s) { }
После: public void addStudent(Student student) { }

Почему: Java Convention требует camelCase для методов.
Источник: https://google.github.io/styleguide/javaguide.html#s5.3-camel-case

### 2. Пробелы после if/for/while

До:    if(condition) {
После: if (condition) {

Почему: улучшает читаемость, отделяет ключевое слово от выражения.
Источник: Oracle Code Conventions — Whitespace

### 3. Длина строки: максимум 120 символов

До:    public List getStudentsFromSpecificCityWithVeryLongName...
После: public List getStudentsByCity(String city) {

Почему: длинные строки затрудняют чтение в редакторе и при code review.
Источник: https://google.github.io/styleguide/javaguide.html#s4.4-column-limit

### 4. Порядок импортов

До:    import java.util.List; import java.util.ArrayList; import java.io.File;
После: import java.io.File; import java.util.ArrayList; import java.util.List;

Почему: алфавитный порядок упрощает поиск импортов.
Источник: IntelliJ IDEA → Code → Optimize Imports

### 5. Фигурные скобки для if

До:    if (condition) doSomething();
После: if (condition) { doSomething(); }

Почему: скобки обязательны даже для однострочных блоков.
Источник: https://google.github.io/styleguide/javaguide.html#s4.1.1-braces-always-used

##Дополнительные материалы

## Code Review Checklist

Используйте этот чеклист для само-ревью перед запросом ревью у ментора:

## Примеры Code Review комментариев

### Хорошие комментарии (конструктивные)

**Пример 1:**

**Проблема:** Метод `calculateDiscount` (строка 45) имеет 3 вложенных if-else и 40 строк.
**Почему это важно:** Сложная логика плохо тестируется и тяжело поддерживается.
**Предложение:** Вынести каждое условие в отдельный метод (например, `isEligibleForBonusDiscount()`)
и использовать паттерн Strategy для разных типов скидок.



**Пример 2:**

**Проблема:** Тест `testProcessOrder` (строка 78) проверяет только успешный сценарий.
**Почему это важно:** Не проверена обработка ошибок при недостаточном балансе.
**Предложение:** Добавить тест `testProcessOrder_InsufficientBalance_ThrowsException()`
с использованием `assertThatThrownBy()`.



### Плохие комментарии (неконструктивные)

**Пример 1:**

Этот код ужасен, полностью переписать.


**Почему плохо:** Нет конкретики (что именно плохо), нет предложения (как исправить),
токсичный тон (демотивирует автора).

**Пример 2:**

Здесь лучше использовать Stream API.


**Почему плохо:** Нет объяснения почему лучше, нет примера как переписать,
неясно какую проблему это решает.

## Информационный поиск — результаты DVT-11

### Запросы и источники

| № | Запрос              | Операторы                                            | Официальный источник                                                   | Альтернатива                                                            | Статус | Дата проверки |
|---|---------------------|------------------------------------------------------|------------------------------------------------------------------------|-------------------------------------------------------------------------|--------|---------------|
| 1 | Lombok Gradle Short | site:search.maven.org "lombok"                       | https://central.sonatype.com/artifact/org.projectlombok/lombok/1.18.42 | https://mvnrepository.com/artifact/org.projectlombok/lombok/1.18.42     | 200 OK | 04.02.2026    |
| 2 | Java 25 Stream API  | site:docs.oracle.com "stream api" intitle:api        | https://docs.oracle.com/en/java/javase/25/docs/api/                    | https://jdk.java.net/25/                                                | 200 OK | 04.02.2026    |
| 3 | IntelliJ IDEA       | site:www.jetbrains.com/help "keymap" "intellij idea" | https://www.jetbrains.com/help/idea/settings-keymap.html               | https://sourceforge.net/projects/intellij-community.mirror/files/       | 200 OK | 04.02.2026    |

### AI-промпты и сравнение ассистентов

#### Промпт 1: План решения

Задача: Нужно добавить метод для высчитывания стипендии в класс Student на основе оценок в этом семестре, так же нужно проверить отсутствие пересдач и превышение сроков сдачи
Контекст: Java 25, у меня есть класс Student с полем grant (double), проект на Gradle
Ограничения: без использования регулярных выражений на данном этапе, простая проверка наличия @ и точки
Что мне нужно от тебя: план решения из 3-5 шагов с объяснением "почему". НЕ пиши код — я реализую сам.
Критерии успеха: метод лишает стипендии штрафников и просрочивших сдачу

**AI-1 ([Deeepseek]):**
---

### План решения

**Шаг 1: Дополнить модель данных студента**
*   **Что:** Добавить в класс `Student` поля для хранения информации, необходимой для проверки новых критериев.
*   **Почему:** Исходный класс содержит только размер стипендии (`grant`), но для расчета по новым правилам нужны данные об успеваемости и дисциплине в текущем семестре. Без этих данных метод не сможет принять решение.
*   **Что конкретно добавить:**
    1.  Коллекцию (например, `List<Integer>`) для хранения оценок за текущий семестр.
    2.  Логический флаг (например, `boolean hasRetakes`) или коллекцию для отметки о наличии неудовлетворительных оценок, требующих пересдачи.
    3.  Логический флаг (например, `boolean missedDeadlines`) для фиксации факта просроченных сдач.

**Шаг 2: Спроектировать и реализовать метод расчета стипендии**
*   **Что:** Создать метод (например, `calculateGrant()` или `updateGrant()`) в классе `Student`.
*   **Почему:** Это прямое требование задачи. Метод будет центральной точкой, где применяется вся логика принятия решения о выплате.
*   **Какой будет логика метода:**
    1.  **Первичная проверка на штрафы:** В самом начале метод должен проверить флаги `hasRetakes` и `missedDeadlines`. Если хотя бы один из них имеет значение `true` — стипендия обнуляется (`grant = 0`), и дальнейший расчет не требуется. Это соответствует условию «лишает стипендии штрафников».
    2.  **Расчет по успеваемости:** Если штрафов нет, метод переходит к анализу коллекции оценок. Логика расчета на основе среднего балла (например, повышенная стипендия за отличные оценки, обычная — за хорошие, отсутствие — за удовлетворительные) определяется вашими внутренними правилами.

**Шаг 3: Определить механизм обновления статусов студента (наличие пересдач и просрочек)**
*   **Что:** Продумать, как и когда будут устанавливаться флаги `hasRetakes` и `missedDeadlines`.
*   **Почему:** Эти флаги — ключевые для метода из Шага 2. Они должны быть достоверными. Их можно устанавливать либо вручную через сеттеры, либо (лучше) автоматически на основе других операций.
*   **Как это может работать:**
    *   При добавлении новой оценки (например, через метод `addGrade(int grade)`) проверять, является ли она неудовлетворительной. Если да — автоматически устанавливать `hasRetakes = true`.
    *   При отметке о сдаче работы (например, через метод `submitAssignment(Date submissionDate, Date deadline)`) сравнивать даты. Если `submissionDate` позже `deadline`, устанавливать `missedDeadlines = true`.

**Шаг 4: Интегрировать метод расчета в жизненный цикл**
*   **Что:** Решить, когда будет вызываться метод `calculateGrant()`.
*   **Почему:** Стипендия должна рассчитываться актуально. Её нецелесообразно пересчитывать при каждом обращении к полю `grant`.
*   **Варианты:**
    1.  Вызывать метод вручную (например, в конце семестра).
    2.  Вызывать его автоматически каждый раз при изменении состояния студента (при добавлении оценки или отметке о просрочке) и сохранять результат в поле `grant`.
    3.  Сделать поле `grant` вычисляемым — убрать его как поле класса, а вместо этого создать геттер `getGrant()`, который будет вызывать логику расчета и возвращать актуальное значение «на лету».

**Шаг 5: Протестировать логику**
*   **Что:** Создать сценарии для проверки корректности работы метода.
*   **Почему:** Чтобы убедиться, что система штрафов имеет наивысший приоритет и работает без ошибок.
*   **Ключевые тестовые кейсы:**
    1.  Студент с хорошими оценками, но флагом `missedDeadlines = true` → стипендия 0.
    2.  Студент без нарушений, но с плохими оценками → стипендия понижена или 0 (согласно правилам из Шага 2).
    3.  Студент без нарушений с отличными оценками → повышенная стипендия.
    4.  Проверка последовательности: добавление неудовлетворительной оценки после расчета → при следующем расчете стипендия должна быть обнулена.


**AI-2 ([ChatGPT]):**
---

### 1. Определить входные данные для расчёта стипендии

**Что сделать:**
Чётко зафиксировать, **из каких данных** метод будет считать стипендию:

* оценки за текущий семестр;
* информация о пересдачах;
* информация о сроках сдачи (дедлайны vs фактические даты).

**Почему:**
Без явного набора входных данных метод станет «магическим» и хрупким.
Плюс, это сразу заставляет решить архитектурный вопрос:

* метод берёт данные из полей `Student`,
* или принимает их параметрами (часто проще тестировать).

---

### 2. Проверить дисквалифицирующие условия (ранний выход)

**Что сделать:**
В начале метода последовательно проверить:

* есть ли **пересдачи**;
* есть ли **превышение сроков сдачи**.

Если хотя бы одно условие нарушено → стипендия = 0.

**Почему:**
Это ключевой критерий успеха задачи.
Ранний выход:

* упрощает логику;
* делает код читаемым;
* исключает ситуацию, когда штрафник всё ещё участвует в расчёте по оценкам.

---

### 3. Рассчитать итоговую оценочную базу

**Что сделать:**
Если студент прошёл проверки:

* определить правило расчёта (средний балл, минимальная оценка, диапазоны);
* подготовить числовую основу для вычисления `grant`.

**Почему:**
Расчёт стипендии должен быть **отделён от проверок**.
Так легче:

* менять формулу в будущем;
* добавлять новые типы стипендий;
* писать юнит-тесты.

---

### 4. Назначить или обновить поле `grant`

**Что сделать:**
На основе результата шага 3:

* либо установить рассчитанную сумму;
* либо явно зафиксировать ноль.

Реши, должен ли метод:

* **возвращать значение**,
* или **мутировать поле `grant`**.

**Почему:**
Явное управление состоянием `Student` важно для предсказуемости:

* в Gradle-проекте это сильно упрощает тестирование;
* избегает «плавающих» значений при повторных вызовах метода.

---



**Сравнение:**
--
Оба ответа описывают корректный архитектурный подход к расчёту стипендии, но делают разные акценты. 
Deeepseek вариант более детализированный и «пошаговый»: он глубже погружается в модель данных Student, жизненный цикл объекта и интеграцию логики, 
что хорошо подходит для проектирования системы целиком. Ответ GPT более компактный и архитектурно «чистый»: он фокусируется на более абстрактных моментах реализации задачи.


#### Промпт 2: Проверка кода

**Код для проверки:**

**Что проверить:**
1. Логические ошибки
2. Соответствие Code Conventions
3. Производительность

**AI-1 ([Deeepseek]):**
---
### 1. Логические ошибки и граничные случаи

**Проблема 1: Уязвимость к `NullPointerException` в `getStudentsByCity()`**
*   **Что:** Метод `s.city().equals(city)` вызовет NPE, если у какого-либо студента `city() == null`. Валидация `student != null` в `addStudent()` не гарантирует, что поле `city` у студента не-null.
*   **Как исправить:** Перевернуть сравнение: `city.equals(s.city())` (тогда `s.city()` может быть `null`, и сравнение вернет `false` без ошибки). **Лучшее решение:** Явно обработать null-случай: `filter(s -> s.city() != null && s.city().equals(city))`.

**Проблема 2: Валидация `city` в `getStudentsByCity()`**
*   **Что:** Сам входной параметр `city` может быть `null`. Тогда `s.city().equals(city)` или `city.equals(s.city())` упадет с NPE.
*   **Как исправить:** Добавить проверку в начале метода: `if (city == null) return List.of();` или бросить осмысленное исключение (например, `IllegalArgumentException`).

**Проблема 3: Потенциальная проблема с коллекцией `studentList` (опционально)**
*   **Что:** Поле `studentList` инициализируется в конструкторе, но если кто-то наследуется от класса, возможен доступ к неинициализированному полю. Это редкий случай, но хорошая практика — инициализация поля при объявлении (`private List<Student> studentList = new ArrayList<>();`).

### 2. Соответствие Java Code Conventions

**Проблема 4: Именование метода для получения поля**
*   **Что:** `s.city()` похоже на геттер класса `Student`. По конвенциям, геттер для поля `city` должен называться `getCity()`. Если это метод `city()`, он нарушает стандартные соглашения, но это может быть запланировано (например, record). **Уточните:** Это ваш класс `Student`? Если да — переименуйте метод в `getCity()`.
*   **Как исправить:** В классе `Student` переименовать метод `city()` в `getCity()`.

**Проблема 5: Именование класса (опционально, но важно)**
*   **Что:** `StudentList` — неинформативное имя для класса-контейнера. По названию непонятно, это утилита, хранилище или сервис. Имена `StudentRegistry`, `StudentGroup`, `StudentService` или `StudentRepository` больше отражают назначение.
*   **Как исправить:** Переименовать класс в соответствии с его ответственностью в проекте.

**Проблема 6: Форматирование (минорная)**
*   **Что:** Строка `filter(s -> s.city().equals(city)).` — точка в конце строки выглядит как опечатка. Должно быть `filter(s -> s.city().equals(city))` (без точки перед `toList()`).
*   **Как исправить:** Удалить лишнюю точку: `.filter(s -> s.city().equals(city))`.

### 3. Производительность

**Проблема 7: O-нотация методов**
*   **Что:**
    *   `addStudent(Student student)`: **O(1)** (амортизированное время для `ArrayList.add()`). Это оптимально.
    *   `getStudentsByCity(String city)`: **O(n)**, где n — количество студентов. Это неизбежно для операции фильтрации по неиндексированному полю.
*   **Как можно улучшить (если частые поиски по городу):** Использовать `Map<String, List<Student>>` (город -> список студентов) для поиска за **O(1)**. Но это усложнит добавление/удаление и увеличит потребление памяти. Решение зависит от частоты операций.

**Итог по производительности:** Для небольшого/среднего количества студентов (`n < 10_000`) текущее решение **вполне адекватно**. Проблем с производительностью в коде нет.

---

### Краткий список исправлений:

1.  **Обязательно:** В `getStudentsByCity` добавить защиту от `null` для `s.city()` и входного параметра `city`.
2.  **Обязательно:** Исправить опечатку с лишней точкой в цепочке stream.
3.  **Рекомендуется:** Переименовать метод `city()` в `getCity()` в классе `Student` (если это ваш код).
4.  **Рекомендуется:** Рассмотреть более точное имя для класса `StudentList`.
5.  **Опционально:** Инициализировать поле `studentList` при объявлении.

**AI-2 ([ChatGPT]):**
---
Хороший, аккуратный код — видно, что ты уже думаешь в сторону чистоты 👍 Ниже разбор строго по пунктам, **без переписывания**, только проблемы и как их исправить.

---

## 1. Логические ошибки и граничные случаи

**Проблема 1: `city` может быть `null`**
В `getStudentsByCity(String city)` при вызове `s.city().equals(city)` возможен `NullPointerException`, если:

* `city == null`;
* `s.city() == null`.

**Как исправить:**

* либо явно запретить `null` (проверка аргумента и выброс `IllegalArgumentException`);
* либо использовать безопасное сравнение (`Objects.equals`).

---

**Проблема 2: возможен `null` внутри `studentList`**
Ты защищаешься от `null` в `addStudent`, но:

* список всё равно может быть изменён извне в будущем;
* код `filter(s -> s.city()...)` не защищён от `s == null`.

**Как исправить:**

* либо дополнительно фильтровать `s != null`;
* либо жёстко гарантировать инвариант класса: *в списке никогда нет `null`* (и это задокументировать).

---

**Проблема 3: чувствительность к регистру и пробелам**
`"Moscow"` и `"moscow"` считаются разными городами, так же как `"Moscow"` и `"Moscow "`.

**Как исправить:**

* нормализовать входные данные (например, `trim`, `toLowerCase`);
* или явно указать в контракте метода, что сравнение строгое.

---

## 2. Соответствие Java Code Conventions

**Проблема 1: имя поля `studentList`**
По соглашениям Java:

* если тип уже `List<Student>`, то имя `students` читается лучше и короче.

**Как исправить:**

* переименовать поле для лучшей читаемости (не критично, но рекомендуется).

---

**Проблема 2: форматирование stream-цепочки**
Точки в конце строки — допустимо, но это спорный стиль и часто не проходит code review.

**Как исправить:**

* придерживаться одного стиля по проекту (точка в начале строки — более распространённый вариант).

---

**Проблема 3: отсутствие JavaDoc**
Публичные методы (`addStudent`, `getStudentsByCity`) не описывают:

* можно ли передавать `null`;
* чувствительно ли сравнение к регистру;
* возвращается ли изменяемый список.

**Как исправить:**

* добавить JavaDoc с контрактом метода.

---

## 3. Производительность и O-нотация

**Асимптотика:**
`getStudentsByCity` — **O(n)** по количеству студентов.
Это нормально для линейного поиска.

---

**Проблема 1: повторные вызовы при большом объёме данных**
Если метод вызывается часто и список большой, каждый раз происходит полный проход.

**Как исправить (архитектурно):**

* хранить индекс (`Map<String, List<Student>>` по городу);
* или кэшировать результат, если список редко меняется.

---

**Проблема 2: `toList()` (начиная с Java 16)**
`Stream.toList()` возвращает **неизменяемый список**.
Это может быть неожиданно для вызывающего кода.

**Как исправить:**

* либо явно указать это в документации;
* либо использовать `Collectors.toList()`.

---

### Итог

Код **рабочий и аккуратный**, но:

* есть риск `NullPointerException`;
* контракт методов неявный;
* линейный поиск может стать узким местом при росте данных.

Это не ошибки «новичка», а как раз те моменты, на которые обращают внимание на ревью 🙂

**Сравнение:**
--
Ответ GPT более структурированный и «академичный»: он системно проходит по всем пунктам проверки, делает акцент на контрактах методов, не уходя в конкретные правки строк. 
Ответ Deepseek практичнее и приземлённее — он чаще указывает на конкретные технические детали (NPE, лишняя точка, инициализация поля, naming), которые легко представить на реальном code review. 
При этом второй Deepseek выходит за рамки исходного задания, предлагая фактические правки и предположения о `Student` (record vs класс).
